services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: talent_hub_app
    ports:
      - "8085:8080"
    env_file:
      - .env
    environment:
      - PORT=8080
      - HOST=0.0.0.0
      - NODE_ENV=production
    restart: always
